-- Снимок состояния товара в строке сметы.
--
-- Не join к Part: строка сметы неизменна по устройству (description, unitPrice
-- и total здесь уже снимки), а связь с товаром объявлена SET NULL — товар,
-- который ни разу не заказывали, удалить можно, и строка молча потеряла бы
-- partId. Печатная смета перестала бы говорить «б/у», сохранив цену,
-- согласованную ИМЕННО за б/у, а для б/у состояние и есть предмет согласования.
--
-- Nullable и БЕЗ backfill: строки, созданные до появления б/у, состояния не
-- имели, и приписывать им его задним числом значило бы выдумывать данные.
ALTER TABLE "EstimateLine" ADD COLUMN "conditionSnapshot" "PartCondition";
