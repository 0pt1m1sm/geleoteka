-- Содержание страницы поколения.
--
-- Зачем: страница кузова состояла из справочных полей (код, годы), одинаковых
-- по форме у всех поколений. Яндекс исключил такие страницы как малоценные —
-- 41 из 113 на 31.08.2026. Описание и список слабых мест дают странице то,
-- чего нет у соседней, и превращают её в ответ на реальный запрос
-- («слабые места W463», «ТО W461»).
--
-- Оба поля необязательные: поколения без содержания остаются в каталоге для
-- подбора запчастей, но в индекс не заявляются.
ALTER TABLE "VehicleGeneration" ADD COLUMN "description" TEXT;
ALTER TABLE "VehicleGeneration" ADD COLUMN "commonIssues" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
